import axios from 'axios'
import history from '../history'

// ACTION TYPES

// INITIAL STATE

// ACTION CREATORS

// THUNK CREATORS

// REDUCER
export default function(state = defaultMatches, action) {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
}
